/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ContactBarreComponent } from './contact-barre.component';

describe('ContactBarreComponent', () => {
  let component: ContactBarreComponent;
  let fixture: ComponentFixture<ContactBarreComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [ContactBarreComponent]
    });
    fixture = TestBed.createComponent(ContactBarreComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
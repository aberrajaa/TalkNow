/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { OwnerBoxComponent } from './owner-box.component';

describe('OwnerBoxComponent', () => {
  let component: OwnerBoxComponent;
  let fixture: ComponentFixture<OwnerBoxComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [OwnerBoxComponent]
    });
    fixture = TestBed.createComponent(OwnerBoxComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/